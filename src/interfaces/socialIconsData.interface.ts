import { SvgPropsInterface } from '@interfaces/svgProps.interface';

export interface SocialIconsDataInterface {
    name: string;
    Icon: React.ElementType;
    url: string;
    nickname: string;
}